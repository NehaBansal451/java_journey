package com.OOPS;

public class basics_02 {
     //wrapper class
     //Integer (capital I) is different from int
    //It is a wrapper class (object) for int
    /* Integer x = 10;
     Now it’s an object, not primitive */
    // “Integer swapping doesn’t work because Java passes copies of references, and Integer objects are immutable.”


    /* final int bonus =3;
       bonus =4
       cannot do this here final act as const which means cannot change bonus
     */
}
