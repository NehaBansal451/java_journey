package com.time_complexity;

public class overAll {

    //time complexity : function that gives relation about  how time will grow as input grows
    //hence ignore less dominating terms
    //o(1) << o(logn) << o(n) << o(nlogn) << o(2^n)
    //akaa bazee method to find time complexity
    // again see video or just make notes from claude and all see whether all topics complete or not
}
